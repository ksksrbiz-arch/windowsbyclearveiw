---
import PhotoGuide from './PhotoGuide.astro';

interface Props { heading?: string | null; }
const { heading = 'Insert or full-frame? See the difference in a real opening.' } = Astro.props;
---
<PhotoGuide type="installation" heading={heading} />
