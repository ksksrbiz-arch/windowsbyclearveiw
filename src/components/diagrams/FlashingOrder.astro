---
import PhotoGuide from './PhotoGuide.astro';

interface Props { heading?: string | null; }
const { heading = 'Window flashing: see the work, then follow the layers.' } = Astro.props;
---
<PhotoGuide type="flashing" heading={heading} />
