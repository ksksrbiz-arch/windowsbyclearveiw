---
import PhotoGuide from './PhotoGuide.astro';

interface Props { heading?: string | null; }
const { heading = 'Signs of trouble: start with what you can actually see.' } = Astro.props;
---
<PhotoGuide type="symptoms" heading={heading} />
