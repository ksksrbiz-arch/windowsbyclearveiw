---
import PhotoGuide from './PhotoGuide.astro';

interface Props { heading?: string | null; }
const { heading = 'What changes the price? See the scope behind the number.' } = Astro.props;
---
<PhotoGuide type="cost" heading={heading} />
