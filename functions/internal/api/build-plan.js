import { AUTHORITIES, BUILD_PLAN_KNOWLEDGE_VERSION, INSTALL_RULES, PRODUCT_SOURCES, QC_RULES, lintPlan, materialPlan, sourceSnapshot } from '../../_lib/build-plan-rules.mjs';
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'private, no-store'}});}
async function ensureSchema(db){await db.prepare(`CREATE TABLE IF NOT EXISTS quote_build_plans (quote_id TEXT PRIMARY KEY REFERENCES quotes(id) ON DELETE CASCADE, version INTEGER NOT NULL DEFAULT 1, plan_json TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, updated_by TEXT NOT NULL DEFAULT 'mark')`).run();for(const sql of [`ALTER TABLE quote_build_plans ADD COLUMN source_json TEXT`,`ALTER TABLE quote_build_plans ADD COLUMN quality_json TEXT`,`ALTER TABLE quote_build_plans ADD COLUMN knowledge_version TEXT`,`ALTER TABLE quote_build_plans ADD COLUMN finalized_at TEXT`,`ALTER TABLE quote_build_plans ADD COLUMN state TEXT NOT NULL DEFAULT 'draft'`,`ALTER TABLE quote_build_plans ADD COLUMN approved_at TEXT`,`ALTER TABLE quote_build_plans ADD COLUMN approved_by TEXT`]){try{await db.prepare(sql).run();}catch{}}}
function clean(value,max=1000){return typeof value==='string'?value.trim().slice(0,max):'';}
function inferPlanItem(item,openingNumber){const label=clean(item.label,300),lower=label.toLowerCase();const method=/full[- ]?frame|full frame/.test(lower)?'full-frame':/new construction|new build|construction/.test(lower)?'new-construction':'verify';const type=/slider|sliding/.test(lower)?'slider':/casement/.test(lower)?'casement':/picture|fixed/.test(lower)?'picture/fixed':/awning/.test(lower)?'awning':/door/.test(lower)?'sliding door':/double[- ]?hung/.test(lower)?'double-hung':'unknown';return{id:`opening-${openingNumber}`,opening:openingNumber,sourceItemLabel:label||'Quoted opening',product:label||'Quoted opening — VERIFY order confirmation',quantity:1,openingType:type,installationMethod:method,existingCondition:'VERIFY at site',dimensions:'VERIFY at site',flashing:'VERIFY against selected product instructions and opening water-management conditions',notes:'',qc:[...QC_RULES]};}
function matchedSources(items){const text=items.map(i=>`${i.label||''} ${i.description||''}`).join(' ').toLowerCase();return PRODUCT_SOURCES.filter(s=>{const m=s.manufacturer.toLowerCase();return text.includes(m.split(' ')[0])||text.includes(m.replace(/\s+windows?$/,''));}).map(s=>s.id);}
export function generatePlan(quote,items){let openingNumber=0;const openings=items.flatMap(item=>Array.from({length:Math.min(50,Math.max(1,Number(item.quantity)||1))},()=>inferPlanItem(item,++openingNumber)));const projectType=/new construction|new build/i.test(`${quote.notes||''} ${items.map(i=>i.label).join(' ')}`)?'new-construction':'replacement-or-verify';const materials=materialPlan({projectType,openings});const verifiedBuy=materials.filter(r=>r.category==='quote-derived'||r.category==='baseline');const deferred=materials.filter(r=>r.category==='verify'||r.category==='conditional'||r.category==='site-derived');const sourceIds=matchedSources(items);return{schemaVersion:2,knowledgeVersion:BUILD_PLAN_KNOWLEDGE_VERSION,authorities:AUTHORITIES,productSources:PRODUCT_SOURCES,matchedProductSourceIds:sourceIds,project:{customer:clean(quote.customer_name,200),address:clean(quote.customer_address,300),city:clean(quote.customer_city,120),projectType,totalQuotedCents:Number(quote.total_cents)||0},sourceItems:sourceSnapshot(items),materialRules:materials.map(r=>({id:r.id,name:r.name,category:r.category,trigger:r.trigger})),buy:verifiedBuy.map(r=>`${r.name} — ${r.category}: ${r.action}`),load:['All quoted units and verified accessories','Verified flashing / sill protection materials','Compatible shims and manufacturer-approved fasteners','Verified sealants and perimeter-gap materials','Approved trim/finish materials actually in scope','Protection, cleanup and required photo equipment'],install:INSTALL_RULES.map(r=>`${r.phase}: ${r.text}`),verify:[...deferred.map(r=>`${r.name} — ${r.category}: ${r.action}`),'Exact unit sizes, configuration and opening assignments verified against the order confirmation','Installation path confirmed for every opening; do not infer insert/full-frame/new-construction solely from product type','Manufacturer-specific installation and flashing instructions available for the selected product','Fastener type, length, diameter, spacing and support requirements verified from product instructions','Flashing/sill/water-management sequence verified for the wall and opening conditions','Sealant compatibility and application requirements verified','Any rot, framing repair, substrate damage or unexpected concealed condition documented before proceeding','Any quantity that cannot be calculated from known measurements remains a VERIFY item'],qc:[...QC_RULES],openings,notes:clean(quote.notes,4000)};}
function sameSource(a,b){return JSON.stringify(a||[])===JSON.stringify(b||[]);}
export async function onRequestGet(context){const{env}=context;await ensureSchema(env.QUOTES_DB);const url=new URL(context.request.url);const quoteId=clean(url.searchParams.get('quoteId'),100);const fresh=url.searchParams.get('fresh')==='1';if(!quoteId)return json({error:'Quote id is required.'},400);const quote=await env.QUOTES_DB.prepare('SELECT * FROM quotes WHERE id = ?').bind(quoteId).first();if(!quote)return json({error:'Quote not found.'},404);const{results:items}=await env.QUOTES_DB.prepare('SELECT * FROM quote_items WHERE quote_id = ? ORDER BY sort_order ASC, id ASC').bind(quoteId).all();const currentSource=sourceSnapshot(items);const saved=await env.QUOTES_DB.prepare('SELECT * FROM quote_build_plans WHERE quote_id = ?').bind(quoteId).first();if(!saved||fresh){const plan=generatePlan(quote,items);plan.status=saved?.state||'draft';plan.approvedAt=saved?.approved_at||null;plan.approvedBy=saved?.approved_by||null;return json({quote,items,plan,generated:true,regenerated:fresh,version:Number(saved?.version||1),stale:false,quality:lintPlan(plan,items),knowledgeVersion:BUILD_PLAN_KNOWLEDGE_VERSION,status:plan.status,approvedAt:plan.approvedAt,approvedBy:plan.approvedBy});}let plan;try{plan=JSON.parse(saved.plan_json);}catch{plan=generatePlan(quote,items);}plan.status=saved.state||plan.status||'draft';plan.approvedAt=saved.approved_at||plan.approvedAt||null;plan.approvedBy=saved.approved_by||plan.approvedBy||null;const savedSource=saved.source_json?(()=>{try{return JSON.parse(saved.source_json);}catch{return[];}})():plan.sourceItems||[];const stale=!sameSource(savedSource,currentSource);const quality=saved.quality_json?(()=>{try{return JSON.parse(saved.quality_json);}catch{return lintPlan(plan,items);}})():lintPlan(plan,items);return json({quote,items,plan,generated:false,version:Number(saved.version)||1,updatedAt:saved.updated_at,stale,quality,knowledgeVersion:saved.knowledge_version||plan.knowledgeVersion||BUILD_PLAN_KNOWLEDGE_VERSION,status:plan.status,approvedAt:plan.approvedAt,approvedBy:plan.approvedBy});}
export async function onRequestPost(context){await ensureSchema(context.env.QUOTES_DB);const{env,request}=context;let body;try{body=await request.json();}catch{return json({error:'Body must be JSON.'},400);}const quoteId=clean(body.quoteId,100);if(!quoteId)return json({error:'Quote id is required.'},400);const quote=await env.QUOTES_DB.prepare('SELECT * FROM quotes WHERE id = ?').bind(quoteId).first();if(!quote)return json({error:'Quote not found.'},404);if(quote.status!=='draft')return json({error:'Only draft quotes can be changed through the build-plan editor.'},409);const{results:items}=await env.QUOTES_DB.prepare('SELECT * FROM quote_items WHERE quote_id = ? ORDER BY sort_order ASC, id ASC').bind(quoteId).all();const plan=body.plan&&typeof body.plan==='object'?body.plan:null;if(!plan)return json({error:'A build plan is required.'},400);const existing=await env.QUOTES_DB.prepare('SELECT state FROM quote_build_plans WHERE quote_id = ?').bind(quoteId).first();if(existing?.state==='approved')return json({error:'Approved Build Plans are locked. Reopen the plan before editing.'},409);plan.schemaVersion=2;plan.knowledgeVersion=BUILD_PLAN_KNOWLEDGE_VERSION;plan.authorities=AUTHORITIES;plan.productSources=PRODUCT_SOURCES;plan.matchedProductSourceIds=matchedSources(items);plan.status=existing?.state||plan.status||'draft';const source=sourceSnapshot(items);const quality=lintPlan(plan,items);if(quality.blockers.length)return json({error:'Build plan needs correction before it can be saved.',blockers:quality.blockers,warnings:quality.warnings,quality},409);plan.sourceItems=source;const safe=JSON.stringify(plan);if(safe.length>100000)return json({error:'Build plan is too large to save.'},413);const now=new Date().toISOString();const version=Number((await env.QUOTES_DB.prepare('SELECT version FROM quote_build_plans WHERE quote_id = ?').bind(quoteId).first())?.version||0)+1;await env.QUOTES_DB.prepare(`INSERT INTO quote_build_plans (quote_id,version,plan_json,source_json,quality_json,knowledge_version,state,created_at,updated_at,updated_by) VALUES (?,?,?,?,?,?,?,?,?,'mark') ON CONFLICT(quote_id) DO UPDATE SET version=excluded.version,plan_json=excluded.plan_json,source_json=excluded.source_json,quality_json=excluded.quality_json,knowledge_version=excluded.knowledge_version,updated_at=excluded.updated_at,updated_by=excluded.updated_by`).bind(quoteId,version,safe,JSON.stringify(source),JSON.stringify(quality),BUILD_PLAN_KNOWLEDGE_VERSION,plan.status,now,now).run();return json({ok:true,version,updatedAt:now,quality,stale:false,status:plan.status});}
